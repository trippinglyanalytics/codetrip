{
  "name": "portfolio-cloud-deployment",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud-ready portfolio app with GitHub Actions CI pipeline.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test tests/smoke.test.mjs",
    "dev": "python3 -m http.server 8080"
  }
}
